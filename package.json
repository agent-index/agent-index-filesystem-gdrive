{
  "name": "@agent-index/filesystem-gdrive",
  "version": "1.0.0",
  "description": "Agent Index Filesystem MCP Server — Google Drive adapter",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "agent-index-filesystem-gdrive": "src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "build": "node scripts/build.js",
    "build:bundle": "esbuild src/index.js --bundle --platform=node --target=node18 --format=esm --outfile=dist/server.bundle.js --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "test": "node --test src/**/*.test.js"
  },
  "keywords": ["agent-index", "mcp", "google-drive", "filesystem"],
  "author": "Agent Index",
  "license": "SEE LICENSE IN LICENSE",
  "dependencies": {
    "@agent-index/filesystem": "file:../agent-index-filesystem",
    "@googleapis/drive": "^8.0.0",
    "@googleapis/oauth2": "^1.0.0",
    "google-auth-library": "^9.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
